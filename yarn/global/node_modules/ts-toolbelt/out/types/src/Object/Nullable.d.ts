import { Merge } from './Merge';
import { Nullable as UNullable } from '../Union/Nullable';
import { Depth } from './_Internal';
import { Pick } from './Pick';
import { Index } from '../_Internal';
import { Implements } from '../Any/Implements';
import { Keys } from './Keys';
declare type NullableFlat<O> = {
    [K in keyof O]: UNullable<O[K]>;
};
declare type NullableDeep<O> = {
    [K in keyof O]: NullableDeep<UNullable<O[K]>>;
};
declare type NullablePart<O extends object, depth extends Depth> = {
    'flat': NullableFlat<O>;
    'deep': NullableDeep<O>;
}[depth];
/** Make some fields of **`O`** nullable (deeply or not)
 * @param O to make nullable
 * @param K to choose fields (?=`keyof O`)
 * @param depth to do it deeply (?=`'flat'`)
 * @returns **`object`**
 * @example
 * ```ts
 * ```
 */
export declare type Nullable<O extends object, K extends Index = keyof O, depth extends Depth = 'flat'> = {
    1: NullablePart<O, depth>;
    0: Merge<NullablePart<Pick<O, K>, depth>, O>;
}[Implements<Keys<O>, K>];
export {};
