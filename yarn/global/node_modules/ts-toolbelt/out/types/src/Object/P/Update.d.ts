import { Compute } from '../../Any/Compute';
import { IterationOf } from '../../Iteration/IterationOf';
import { Iteration } from '../../Iteration/Iteration';
import { Pos } from '../../Iteration/Pos';
import { Next } from '../../Iteration/Next';
import { Path as PPath } from './_Internal';
import { Index } from '../../_Internal';
import { EndOf } from '../../Tuple/EndOf';
declare type _Update<O extends object, Path extends Index[], A, I extends Iteration = IterationOf<'0'>> = {
    [K in keyof O]: Compute<K extends Path[Pos<I>] ? Pos<I> extends EndOf<Path> ? A : _Update<O[K] & {}, Path, A, Next<I>> : O[K]>;
};
/** Update in **`O`** the fields at **`Path`** with **`A`**
 * @param O to update
 * @param Path to be followed
 * @param A to update with
 * @returns **`object`**
 * @example
 * ```ts
 * ```
 */
export declare type Update<O extends object, Path extends PPath, A extends any> = _Update<O, Path, A>;
export {};
