import { Index } from '../_Internal';
/** Get the keys of a **tuple**
 * @param T
 * @returns **`keyof`**
 * @example
 * ```ts
 * ```
 */
export declare type Keys<T extends any[]> = (Exclude<keyof T, keyof any[]> & Index) | number;
