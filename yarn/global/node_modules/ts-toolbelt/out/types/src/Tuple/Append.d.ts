import { Concat } from './Concat';
/** Add an element **`A`** at the end of **`T`**
 * @param T to append to
 * @param A to be added to
 * @returns **`any[]`**
 * @example
 * ```ts
 * ```
 */
export declare type Append<T extends any[], A extends any> = Concat<T, [A]>;
