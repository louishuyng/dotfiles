import { IgniteToolbox } from '../types';
/**
 * Shows a list of known plugins.
 *
 * @param {Object} toolbox The gluegun toolbox.
 */
export default function (toolbox: IgniteToolbox): Promise<void>;
