import { IgniteToolbox } from '../types';
/**
 * Fetches plugin registry from Github
 *
 * @param {Object} toolbox The gluegun toolbox.
 */
export default function (toolbox: IgniteToolbox): Promise<any>;
