import { IgniteToolbox } from '../../types';
export declare type AddModuleOptions = {
    link?: boolean;
    dev?: boolean;
    version?: string;
};
declare const _default: (toolbox: IgniteToolbox) => (moduleName: string, options?: AddModuleOptions) => Promise<void>;
export default _default;
