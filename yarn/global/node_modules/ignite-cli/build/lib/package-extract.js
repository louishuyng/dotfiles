"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Splits an NPM package name into parts.
 */
exports.default = (function (versionString) {
    var scoped = versionString.includes('/');
    var name = versionString;
    var packageParts = versionString.split('@');
    // has version string?
    var version = undefined;
    if (versionString.lastIndexOf('@') > 0) {
        name = packageParts.slice(0, packageParts.length - 1).join('@');
        version = packageParts[packageParts.length - 1];
    }
    return { name: name, scoped: scoped, version: version };
});
//# sourceMappingURL=package-extract.js.map