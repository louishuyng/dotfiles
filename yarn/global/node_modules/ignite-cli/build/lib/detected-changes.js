"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Returns a list of keys whose values have changed.
 */
exports.default = (function (oldObject, newObject) {
    var _a = require('ramda'), keys = _a.keys, intersection = _a.intersection, reduce = _a.reduce, concat = _a.concat;
    var oldKeys = keys(oldObject);
    var newKeys = keys(newObject);
    var inter = intersection(oldKeys, newKeys);
    return reduce(function (acc, k) {
        if (oldObject[k] !== newObject[k]) {
            return concat(["'" + k + "'"], acc);
        }
        return acc;
    }, [], inter);
});
//# sourceMappingURL=detected-changes.js.map