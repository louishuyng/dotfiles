"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Is Ignite compatible with the directory we're currently inside?
 */
function isIgniteDirectory(directory) {
    var jetpack = require('fs-jetpack');
    // read the ignite config
    var igniteConfigPath = directory + "/ignite/ignite.json";
    // it must be a file
    if (jetpack.exists(igniteConfigPath) !== 'file')
        return false;
    // let's read it as a JSON file
    try {
        var contents = jetpack.read(igniteConfigPath, 'json');
        // it needs to be an object
        return typeof contents === 'object';
    }
    catch (err) {
        return false;
    }
}
exports.default = isIgniteDirectory;
//# sourceMappingURL=is-ignite-directory.js.map