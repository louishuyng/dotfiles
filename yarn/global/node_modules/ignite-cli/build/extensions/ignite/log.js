"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var PrettyError = require("pretty-error");
exports.default = (function (toolbox) {
    /**
     * Prints a debug message to the console.  Used when the user wants to run in --debug.
     *
     * @param {string|Object} message - The message to write
     */
    var pe = new PrettyError();
    function log(message) {
        var parameters = toolbox.parameters, print = toolbox.print;
        // jet if we're not running in debug mode
        if (!parameters.options.debug)
            return;
        var date = new Date().toISOString().slice(11, 19);
        var prefix = print.colors.muted(date) + ' ' + print.colors.magenta('[ignite]');
        if (typeof message === 'object') {
            console.log("" + prefix);
            console.log(pe.render(message));
        }
        else {
            console.log(prefix + " " + message);
        }
    }
    return log;
});
//# sourceMappingURL=log.js.map