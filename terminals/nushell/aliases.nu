# Aliases
alias g = git
alias v = nvim
alias tm = tmux
alias zed = ^open -a /Applications/Zed.app -n

alias k = kubectl
alias kns = kubens
alias kctx = kubectx

alias ll = ls -l
alias cdes = rm -rf ~/Desktop/*
